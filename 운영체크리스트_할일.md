# 남은 운영 체크리스트 (To-Do)

이 문서는 배포 및 운영을 위해 아직 확인이나 테스트가 필요한 남은 작업들입니다.
목표는 모르는 사용자가 데스크톱에서 1분 안에 자막을 시작하고, 3분 이상 막히지 않고 써볼 수 있게 만드는 것입니다.

## 0. 현재 우선순위

1. 데스크톱 첫 사용 흐름 안정화
2. 내부 링크, 설정 저장, 콘솔 오류 정리
3. GA/Clarity 이벤트 확인
4. STT provider와 Supabase 운영 설정 검증
5. 모바일은 마지막에 화면 깨짐만 우선 정리

**지금 당장 하지 말 것:**
- 대규모 홍보 / 애드센스 수익 기대
- 유료 사용자 100명, 1,000명 계산
- 모바일 완성도 집착
- 기능을 한 번에 많이 추가하기
- 프리미엄 기능을 급하게 잠그기

**프리미엄 방향:**
- 로컬 실시간 자막은 무료로 유지한다.
- 접근성 핵심 기능을 잠그지 않는다.
- 돈은 고정밀 변환, 회의록, 자막 파일, 저장/공유 운영에서 받는다.
- 프리미엄 작업은 첫 사용 흐름이 안정된 뒤에 한다.

---

## 1. 데스크톱 첫 사용 흐름 (UX 및 오류 QA)

- [ ] 마이크 권한 거부 시 다음 행동을 알려주는지 확인
- [ ] 오류가 나도 사용자가 무엇을 해야 하는지 알 수 있는지 확인
- [ ] 브라우저 콘솔 오류 확인

## 2. 링크와 배포 파일

- [ ] `index.html` 하단 링크 확인
- [ ] `guide.html`, `privacy.html`, `terms.html` 접근 확인
- [ ] `/ads.txt` 접근 확인 및 AdSense publisher ID 일치 확인
- [ ] 문의 이메일이 실제 수신 가능한지 확인
- [ ] `/api/config` 정상 응답 확인
- [ ] 사용자가 누를 수 있는 내부 링크 중 404가 없는지 확인

## 3. GA와 Clarity (데이터 분석)

- [ ] 배포 주소를 직접 열었을 때 GA 실시간에 잡히는지 확인
- [ ] Clarity 세션이 잡히는지 확인
- [ ] Start captions 클릭 이벤트를 추적할 수 있는지 확인
- [ ] Premium 클릭 이벤트를 추적할 수 있는지 확인
- [ ] 로그인 클릭 또는 막힘 지점을 볼 수 있는지 확인
- [ ] 모바일에서 이탈하거나 UI가 불편한 지점을 볼 수 있는지 확인

## 4. STT Provider 실제 API 키 테스트 (배포 환경)

권장 순서대로 짧은 음성(한국어/영어), 무음, 잡음, 잘못된 키 입력 상태를 테스트합니다.

- [ ] **Groq**: 환경변수 `GROQ_API_KEY`, 기대값 `/api/stt`가 `{ text, provider: "groq" }` 반환
- [ ] **OpenAI**: 환경변수 `OPENAI_API_KEY`
- [ ] **Gladia**: 환경변수 `GLADIA_API_KEY` (화자 분리 옵션 우선 테스트)
- [ ] **Speechmatics**: 환경변수 `SPEECHMATICS_API_KEY`
- [ ] **IBM Watson STT**: 환경변수 `IBM_STT_API_KEY` (서비스 URL 필요)
- [ ] **Azure Speech**: 환경변수 `AZURE_SPEECH_KEY` (리전 필요)

## 5. 번역 자막 비용과 지연 (AI API)

- [ ] 1분 사용 시 AI 요청 수
- [ ] 번역 지연 체감
- [ ] 무료 API 한도 소진 속도
- [ ] 번역 실패 시 원문 자막이 계속 유지되는지 확인

## 6. Supabase 공유 자막 정책 (Realtime)

- [ ] `SUPABASE_URL`, `SUPABASE_ANON_KEY`가 배포 환경변수에만 있음
- [ ] Supabase anon key가 service role key가 아님
- [ ] Realtime 기능이 필요한 채널에만 허용됨
- [ ] 민감한 대화 공유 금지 안내가 UI에 표시됨
- [ ] 방 번호 추측 가능성을 운영상 허용할 수 있는지 확인 (개선: 8자 이상 랜덤 코드, 방 만료 등)

## 7. Cloudflare 보안/운영 및 환경변수

**방어 (WAF/Rules):**
- [ ] `/.env`, `/.git/*`, `/config.php` 등 접근 차단
- [ ] `l9scan` 등 User-Agent 차단
- [ ] `45.88.138.45` 등 스캐너 IP 차단

**환경변수 세팅:**
- [ ] `ALLOWED_ORIGINS`
- [ ] 각 STT 제공자 API 키 (`GROQ_API_KEY`, `OPENAI_API_KEY` 등)
- [ ] `SUPABASE_URL`, `SUPABASE_ANON_KEY`

## 8. 모바일 최종 확인 (데스크톱 안정화 이후)

- [ ] 첫 화면에서 가로 스크롤이 없는지 확인
- [ ] 텍스트와 버튼이 겹치지 않는지 확인
- [ ] 자막 패널이 화면 밖으로 밀리지 않는지 확인
- [ ] 주요 버튼을 터치할 수 있는지 확인
- [ ] 데이터 처리 안내가 첫 화면을 과하게 밀어내지 않는지 확인

# 📂 프로젝트 파일 구조 안내

코드를 작성할 때 파일의 역할을 쉽게 파악할 수 있도록 정리한 안내서입니다.
(이 문서는 참고용이므로 마음껏 수정하셔도 됩니다!)

## 📜 핵심 화면 파일
* **`index.html`** : 메인 화면 뼈대 (웹페이지의 전체 구조)
* **`css/style.css`** : 디자인 및 스타일 (색상, 크기, 애니메이션 등)

## 🧩 자바스크립트(JS) 모듈 파일 (역할별 분리)
* **`js/constants.js`** : 고정값 모음 (변하지 않는 설정값, UI 텍스트 데이터 등)
* **`js/settings.js`** : 사용자 설정 (API 키 저장, 폰트 크기/색상 등 환경설정 관리)
* **`js/auth.js`** : 로그인 및 인증 (사용자 로그인, 로그아웃 처리)
* **`js/history.js`** : 기록 관리 (과거 자막 기록 저장 및 불러오기)
* **`js/ui.js`** : 화면 제어 (버튼 클릭, 언어 변경, 안내 메시지 띄우기 등 화면 동작)
* **`js/audio.js`** : 음성 및 오디오 (마이크 켜기/끄기, 음성 인식(STT) 처리 등 핵심 기능)
* **`js/workers/local-whisper-worker.js`** : 로컬 음성 인식 워커 (브라우저 내 STT 엔진)

## ⚙️ 서버 및 데이터베이스 (Back-end)
* **`functions/`** : 서버리스 기능 (API 서버 역할). STT 요청, 사용량 체크, 결제 처리 등 보안이 필요한 로직이 들어 있습니다.
    * `api/stt.js` : 음성 인식(STT) 처리용 API
    * `api/usage.js` : 사용자 사용량 관리
* **`database/`** : 데이터베이스 관련 파일. 테이블 설계(Schema) 및 SQL 쿼리 파일이 들어 있습니다.

## 📂 기타
* **`docs/`** : 안내 및 약관 문서 모음 (`guide.html`, `privacy.html`, `terms.html`)
* **`백업/`** : 이전 코드나 임시로 보관할 파일들 (AI 검색에서 제외됨)
* **`운영체크리스트.md`** : 개발 및 운영 진행 상황 관리 (할 일 및 완료 항목 통합)
